# 《失焦》宣传片大纲

> 正片 100 秒，另附 60 秒短版剪法。
> 全片零旁白，仅档案字卡与音效。
> 制作前请先读文末「需要先决定的两件事」。

---

## 一句话概念

**一部机位几乎不动的预告片：世界在固定构图里持续漂移，直到玩家亲手让它停下来。**

延时摄影不只是开场的一个手法，它是整片的结构——因为「观测者」做的事情就是看：从固定的一点，看着一切发生。摄像机替观众扮演观测者。

---

## 三条规定

1. **全片只允许一次镜头运动。** 其余所有镜头都是固定机位。
2. **唯一的那次运动留给登座仪式。** 那是玩家第一次「动手干预」而不是「观看」的时刻。规则在这里破例，观众会记住。破例之后，最后一个镜头回到片头第一个机位——同一个坐标、同一个构图，但含义变了：从档案的视角变成观测者的视角。
3. **每幕之间插档案编号卡。** 黑底白字、逐字打出：
   `记录 01 · 主世界 · 阶段 一` ／ `记录 04 · 末地 · 虚空`
   这既是最省力的 SCP 调性载体，也让「固定机位」看起来是制度，而不是偷懒。

---

## 分幕

### 幕 0 · 冷开场 ｜ 0:00–0:06

| | |
|---|---|
| 画面 | 黑场 |
| 声音 | 低频嗡鸣；底噪里混一点电子合成乐音 |
| 字卡 | `观测者阵列于 10/02/2021 下线。` |

> 可选：铺一段「人类聚居地广播」的合成语音，照源文的广播体写——
> 「幸存的人类请注意。如你能听到这条消息，请立刻随墙上的基金会标志指引，前往最近的庇护站。」
> 比旁白高级得多，而且完全忠于原著。要做就做成听不清、像隔着墙的那种。

### 幕 1 · 延时：世界在同一个框里坏掉 ｜ 0:06–0:26

| | |
|---|---|
| 机位 | 主世界，固定。构图要同时框进地表、树和一点人造建筑 |
| 画面 | **同机位延时摄影。每 100 tick 取一帧（= 方块的一个结算周期）。** |

三段递进，中间用档案卡切开：

- `阶段 一` —— 草方块、木头、石头零星换材质，像信号不良
- `阶段 二` —— 变化密度肉眼可见地上升，地形轮廓还在，表面已经陌生
- `阶段 三` —— 每一帧都在变；山还是那座山，材料已经全换了

**构图一秒都不能变。** 观众靠「同一个框」察觉自己在被加速推进时间——这是全片的判据。

- 声音：底噪每切一次阶段升一个音级
- 字卡：`事物的语义发生随机漂移。` → `表现为物质形态的不可逆改变。` → `它只会越来越快。`

### 幕 2 · 看见什么，就得到什么 ｜ 0:26–0:42

| | |
|---|---|
| 机位 | 主世界，固定近景（准星一格宽） |
| 画面 | 生存模式。三个动作，**同一机位，靠剪辑点推进，不靠移动** |

1. 挖一块「钻石矿」→ 掉落钻石（那底下原本是泥土）
2. 右键一个「工作台」→ 真的打开合成界面
3. 中键选取 → 拿到「钻石矿」本身

- 声音：干声。挖掘、破坏、界面打开的木头声；环境音压到底
- 字卡：`看见什么，就得到什么。`

> 全片只有这一句是「直给卖点」，其余全交给画面。这一句值 16 秒。

### 幕 3 · 一座基座大小的静止 ｜ 0:42–0:58

| | |
|---|---|
| 机位 | 主世界，固定，略高，完整框住基座的作用范围 |

- 画面：
  - 先给 3 秒合成台特写（四支铁脚、四份红石、一枚末影之眼），说明它是做出来的
  - 然后是**全片最重要的一个镜头**：范围外方块持续漂移，范围内完全静止。至少给 6 秒，不要剪
  - 玩家走进范围——脚步声还在，环境音在穿过边界的一瞬间被切掉；走出去，声音回来
- 声音：进圈静音（低通 + 抽掉环境音）。**让声音也有一个作用范围**
- 字卡：`现场唯一可部署的现实稳定装置。` → `它能起作用，是因为它知道往哪里看。`

### 幕 4 · 它得先学会指认 ｜ 0:58–1:14

| | |
|---|---|
| 机位 | 主世界固定近景，训练终端前 |

- 画面：
  - 手持模型右键方块 / 生物，「已记录」一行行浮出
  - 回终端，「完成训练」→ 概念与完备度 q 出现在界面上
  - 切七枚碎片快切：每枚 0.6 秒，图标 + 一句台词
- 声音：终端的纸张与机械声；碎片快切处每次一个短促电子音
- 字卡（碎片段，快速逐条）：

  ```
  “玫瑰，是玫瑰，是玫瑰……”
  “它不是能用语言描述的事物。”
  “真正的理解。”
  “把所有东西都压到 42ms 内。”
  “一小块 Dr. Kacper Darlin”
  “肾上腺素”
  ```

  > 第七句「世界会继续美丽」不要用在这里，它是片尾的。

- 可选插入（2 秒）：生物稳定模型在工作——周围生物倒下，基座还在运行。道德上不舒服，正是要的效果。

### 幕 5 · 登座 ｜ 1:14–1:28

| | |
|---|---|
| 机位 | **全片唯一一次镜头运动。** 从虚空远景缓慢推向王座 |

- 画面：末地虚空带，四根角柱的光束；玩家落上基座，右键，仪式开始；波次生成；玩家留在原地不动
- 声音：传送门音效、龙息、低音鼓点随波次加密
- 字卡：`王座在末地主岛周边的虚空中。` → `执行者必须在场。完成之前一步不离。`

> 最后那句是整部片子里最重的一句台词，给它留白，不要加音乐。

### 幕 6 · 熄灭 ｜ 1:28–1:36

| | |
|---|---|
| 机位 | 固定，正对观测者核心（这次是「核心在看你」的角度） |

- 画面：安装已完成的 OBSR-3 → 核心点亮 → **画面里的漂移停了**。
  这个「停」必须用同一机位、同一构图拍到：镜头不动，方块不动。观众会自己明白发生了什么。
- 声音：核心点亮的一瞬间，所有环境音抽干，只剩一个持续音
- 字卡：`失焦终止。`

### 幕 7 · 回到第一帧 ｜ 1:36–1:46

| | |
|---|---|
| 机位 | **与幕 1 完全相同的坐标与构图** |

- 画面：一模一样的世界，一模一样的框。区别只有一个：它不再变化。
  粒子还在飘，草还在动，但方块停在原地。
- 声音：留几秒近乎无声
- 字卡：`终止不是修复。已经漂移的东西不会回来。` → `世界会停在你完成它的那一刻。`
- 标题卡：**失焦** ／ FOCAL DECAY

> 标题动效建议：字先以「失焦」出现，笔画小幅错位漂移（像方块换材质），再对齐、定住。
> 片尾那一拍再出现时，它已经是稳定的。

### 幕 8 · 片尾一拍 ｜ 1:46–1:50

- 画面：标题退去，还是那个机位，静止
- 字卡：`“世界会继续美丽。”`
- 黑场

---

## 声音设计要点

- **底噪是主角。** 一条低频嗡鸣贯穿全片，阶段越高越紧、越密。
- **中间那段广播体合成语音**只在幕 0 出现一次，之后不要再出现人声。
- **静音是最贵的效果。** 幕 3 的进圈、幕 6 的熄灭，都是靠「抽掉声音」而不是「加上声音」。
- 不要在幕 5 用史诗配乐。王座那一段的力度来自「玩家站着不动」，不是来自铜管。

---

## 拍摄与制作备注

### 延时怎么拍

- **方块突变的周期是固定 100 tick（5 秒），阶段只改概率，不改周期。** 所以「每 100 tick 截一帧」就是最干净的延时：一帧 = 一次结算。
- 默认概率是 `阶段一 1% / 阶段二 30% / 阶段三 90%`。阶段一直接拍几乎看不出变化。两个办法：
  - `/focaldecay days 7` 直接进阶段三 —— 最快
  - 或临时把 `block_mutation_chance_stage1/2/3` 全设成 `1.0`，让每个周期都变，后期再把帧率拉慢，做出「三次加速」的节奏
- **不要靠等真实时间拍阶段递进**（一个游戏日 = 20 分钟）。用 `/focaldecay days 0 / 3 / 7` 分三段拍，后期接起来。
- 天数在**玩家数为 0 时暂停**。挂机拍延时时人必须在世界里；或者干脆全程用 `/focaldecay days <n>` 手动设。

### 幽灵方块的覆盖范围（最容易穿帮的地方）

客户端只把玩家**视锥内、8 区块（128 格）以内**的方块替换成失焦目标，而且扫描是分帧的：每次 12 个区块节，默认 2 tick 一次。后果两条：

1. **镜头不能拉太远。** 远景里超过 128 格的方块不会失焦，会露馅。把主体压在 4~6 区块内。
2. **每换一个机位，先静止录 10~20 秒再开拍。** 转头、切镜都会重建扫描队列，铺满视锥需要时间（上限约半分钟，实际通常几秒）。

需要更大范围只能改 `max_render_distance`，但硬上限就是 8 区块，改不上去。

### 交互镜头的两个坑

- **创造模式挖方块不会转换**（刻意设计：创造挖掘不产生掉落，无需转换）。幕 2 的三连必须用**生存模式**拍。
- 但**创造模式右键会转换**。所以「右键工作台」这条创造、生存都能拍；「挖钻石掉钻石」只能生存。

### 手册镜头

新世界进服自动发《失焦事件现场作业手册》（需装 Patchouli），翻页镜头直接用这本书，不用另做美术。《王座协议》《重聚焦》两卷有位置锁，`/focaldecay unlock` 可立即解锁，方便拍那两卷。

### 其他

- 王座坐标由种子决定，650~905 格，用 `/focaldecay throne` 查。固定种子 `20260912` 时在 `-752 70 45`。
- 后处理强度 `post_intensity`（默认 1.0）可以调高调低，但它是**全局开关，不能按镜头切**，所以不同强度要在不同录制批次里拍。
- 关 HUD 用 `F1`。
- **不要写「你，能撑过七天吗？」这类预告片文案。** 整片的语气是档案，不是营销。字卡全部用陈述句。

---

## 需要先决定的两件事

**1. 观测者上线之后，`observer_veil` 后处理其实还在跑。**
现在的门控只看 `postProcessEnabled`、level 和原版 effect，**没有看 `observerOnline`**。也就是说结局之后画面仍然是「不安定」的——而主题上那一刻画面应该**变干净**。

- 对宣传片：幕 6「熄灭」是全片的转折点，如果画面还在呼吸，力度会掉一半。
- 两个选择：临时用 `postProcessEnabled=false` 拍幕 6/7；或者干脆给 veil 加上 `observerOnline` 门控。
- **我倾向后者**，它同时是一个玩法上的正反馈：赢了的画面和输了的画面不一样，玩家不用看聊天栏也知道世界变了。

**2. 幕 1 和幕 7 必须是同一个世界、同一个坐标、同一个构图。**
要在同一次录制会话里拍完，所以机位坐标得先定下来写进拍摄脚本（建议先用 `/focaldecay throne` 类似的思路把坐标记死，别靠眼睛找）。

---

## 60 秒短版

| 幕 | 时长 | 取舍 |
|---|---|---|
| 0 + 1 | 0:00–0:14 | 只保留阶段一 → 阶段三两次切换 |
| 2 | 0:14–0:22 | 只留「挖钻石」一条 |
| 3 | 0:22–0:32 | **完整保留**，这是卖点 |
| 4 | 0:32–0:40 | 碎片快切只留三句：玫瑰 / 真正的理解 / 肾上腺素 |
| 5 | 0:40–0:48 | 压缩 |
| 6 + 7 | 0:48–1:00 | **完整保留**，结尾不能省 |
